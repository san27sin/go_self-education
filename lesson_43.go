/*
43. Длина и вместимость слайса
У слайса есть как длина, так и вместимость.

Длина слайса - это количество элементов, которые он содержит.

Вместимость слайса - это количество элементов в базовом массиве, начиная с первого элемента в слайсе.

Длину и вместимость слайса s можно получить с помощью выражений len(s) и cap(s).

Вы можете увеличить длину слайса, повторно его слайся, при условии, что у него есть достаточная вместимость. Попробуйте изменить одну из операций со слайсом в примере программы, чтобы выйти за пределы его вместимости, и посмотрите, что произойдет.
*/

package main

import ("fmt")

func printSlice(s []int) {
	fmt.Printf("len=%d cap=%d %v\n", len(s), cap(s), s)
}

func makeTheory() {
	s := []int{2, 3, 5, 7, 11, 13}
	printSlice(s)

	// обрежем слайс до нулевой длины
	s = s[:0]
	printSlice(s)

	// увеличим длину обратно
	s = s[:4]
	printSlice(s)

	// сдвинем начало слайса — длина уменьшится, вместимость тоже
	s = s[2:]
	printSlice(s)
}

// Задание:
// 1. Создайте слайс из 6 целых чисел (можно использовать любой другой литерал).
// 2. Выведите его len и cap с помощью printSlice.
// 3. Через повторное слайсирование (реслайсинг) уменьшите длину слайса до 2,
//    затем снова увеличьте её до максимально возможной вместимости (используя cap(s)).
// 4. После каждого шага печатайте слайс через printSlice и словами объясняйте
//    (в комментарии рядом), почему len и cap именно такие.
// 5. Дополнительно: попробуйте выполнить s[:cap(s)+1] и посмотрите на панику
//    "slice bounds out of range" — объясните в комментарии, почему она возникает.
func makeHomework() {
    defer func() {
        if r := recover(); r != nil {
            fmt.Println("поймали панику:", r)
        }
    }()

	// #1
	h := []int{2, 5, 1, 7, 3, 4}
	// #2
	printSlice(h)
	// #3
	h = h[:2]
	printSlice(h) // длину обрезали, вместимость отсается согласно изначальному массивы
	h = h[:cap(h)]
	printSlice(h) // длину увеличили, вместимость отсается согласно изначальному массивы
	// #4
	h = h[:cap(h) + 1] // вышли за рамки созданного массива
}

func main() {
	fmt.Println("Теория")
	makeTheory()
	fmt.Println("\nПрактика")
	makeHomework()
}
